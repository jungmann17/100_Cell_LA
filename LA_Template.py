from neuron import hfrom math import piclass Cell_A:    def __init__ (self):        self.soma = soma = h.Section(name='soma',cell=self)        self.initsoma()    def initsoma (self):        soma = self.soma        soma.nseg = 1        soma.diam = 24.75        soma.L = 25        soma.Ra = 200        soma.cm = 2.5        soma.insert('cadyn'); soma.eca = 120; soma.gcabar_cadyn = 5.5e-4;        soma.insert('leak'); soma.el_leak = -72; soma.glbar_leak = 5.5e-5;        soma.insert('hd');  soma.ghdbar_hd = 1.5e-5; #soma.ehd_hd = -43;        soma.insert('na3'); soma.ena = 45; soma.gbar_na3 = 0.03; soma.ar2_na3 = 1; # soma.sh_na3=10;        soma.insert('nap'); soma.gbar_nap = 0.000142;        soma.insert('kdr'); soma.gbar_kdr = 0.0035; # 0.0015        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHP'); soma.gsAHPbar_sAHP = 3e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002; # 0.02		        for sec in h.allsec():            if h.ismembrane('kdr', sec=sec) or h.ismembrane('kdr', sec=sec):			    soma.ek = -80;            elif h.ismembrane('hd', sec=sec):                soma.ehd_hd = -43class Cell_ADA:    def __init__ (self):        self.soma = soma = h.Section(name='soma',cell=self)        self.initsoma()            def initsoma(self):        soma = self.soma        soma.nseg = 1        soma.diam = 24.75        soma.L = 25        soma.Ra = 200        soma.cm = 2.5        soma.insert('cadyn'); soma.gcabar_cadyn = 5.5e-4; soma.eca = 120;        soma.insert('leakDA'); soma.el_leakDA = -72; soma.glbar_leakDA = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5;        soma.insert('na3DA'); soma.gbar_na3DA = 0.03; soma.ena = 45; soma.ar2_na3DA = 1;        soma.insert('nap'); soma.gbar_nap =  0.000142;        soma.insert('kdrDA'); soma.gbar_kdrDA = 0.0035; soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHP'); soma.gsAHPbar_sAHP = 3e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;class Cell_ANE:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma(self):        soma = self.soma        soma.nseg = 1        soma.diam = 24.75        soma.L = 25        soma.Ra = 200        soma.cm = 2.5        soma.insert('cadyn'); soma.gcabar_cadyn = 5.5e-4; soma.eca = 120;        soma.insert('leak'); soma.el_leak = -72; soma.glbar_leak = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5;        soma.insert('na3'); soma.gbar_na3 = 0.03; soma.ena = 45; soma.ar2_na3 = 1;         soma.insert('nap'); soma.gbar_nap = 0.000142;        soma.insert('kdr'); soma.gbar_kdr = 0.0035; soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHPNE'); soma.gsAHPbar_sAHPNE = 3e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;class Cell_ADANE:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma(self):        soma = self.soma        soma.nseg = 1        soma.diam = 24.75        soma.L = 25        soma.Ra = 200        soma.cm = 2.5        soma.insert('cadyn'); soma.gcabar_cadyn = 5.5e-4; soma.eca = 120;        soma.insert('leakDA'); soma.el_leakDA = -72; soma.glbar_leakDA = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5;        soma.insert('na3DA'); soma.gbar_na3DA = 0.03; soma.ena = 45; soma.ar2_na3DA = 1;         soma.insert('nap'); soma.gbar_nap =  0.000142;        soma.insert('kdr'); soma.gbar_kdr = 0.002; soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHP'); soma.gsAHPbar_sAHP = 1.5e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;class Cell_B:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma (self):        soma = self.soma        soma.nseg = 1        soma.diam = 24.75        soma.L = 25        soma.Ra = 200        soma.cm = 2.5        soma.insert('cadyn'); soma.eca = 120; soma.gcabar_cadyn = 5.5e-4;        soma.insert('leak'); soma.el_leak = -72; soma.glbar_leak = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5; # soma.ehd_hd = -43;        soma.insert('na3'); soma.ena = 45; soma.gbar_na3 = 0.027; soma.ar2_na3 = 1; # soma.sh_na3 = 10;        soma.insert('nap'); soma.gbar_nap = 0.000142;        soma.insert('kdr'); soma.gbar_kdr = 0.002; # 0.0015 soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHP'); soma.gsAHPbar_sAHP = 1.5e-4; # 2e-4        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002; # 0.02		        for sec in h.allsec():            if h.ismembrane('kdr', sec=sec) or h.ismembrane('kdr', sec=sec):			    soma.ek = -80;            elif h.ismembrane('hd', sec=sec):                soma.ehd_hd = -43        class Cell_BDA:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma(self):        soma = self.soma        soma.nseg = 1;        soma.diam = 24.75;        soma.L = 25;        soma.Ra = 200;        soma.cm = 2.5;        soma.insert('cadyn'); soma.gcabar_cadyn = 5.5e-4; soma.eca = 120;        soma.insert('leakDA'); soma.el_leakDA = -72; soma.glbar_leakDA = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5;        soma.insert('na3DA'); soma.gbar_na3DA = 0.027; soma.ena = 45; soma.ar2_na3DA = 1;         soma.insert('nap'); soma.gbar_nap = 0.000142;        soma.insert('kdrDA'); soma.gbar_kdrDA = 0.002; soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHP'); soma.gsAHPbar_sAHP = 1.5e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;class Cell_BNE:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma(self):        soma = self.soma        soma.nseg = 1;        soma.diam = 24.75;        soma.L = 25;        soma.Ra = 200;        soma.cm = 2.5;        soma.insert('cadyn'); soma.gcabar_cadyn = 5.5e-4; soma.eca = 120;        soma.insert('leak'); soma.el_leak = -72; soma.glbar_leak = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5;        soma.insert('na3'); soma.gbar_na3 = 0.027; soma.ena = 45; soma.ar2_na3 = 1;         soma.insert('nap'); soma.gbar_nap = 0.000142;        soma.insert('kdr'); soma.gbar_kdr = 0.002; soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHPNE'); soma.gsAHPbar_sAHPNE = 1.5e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;class Cell_BDANE:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma (self):        soma = self.soma        soma.nseg = 1;        soma.diam = 24.75;        soma.L = 25;        soma.Ra = 200;        soma.cm = 2.5;        soma.insert('cadyn'); soma.gcabar_cadyn = 5.5e-4; soma.eca = 120;        soma.insert('leakDA'); soma.el_leakDA = -72; soma.glbar_leakDA = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5;        soma.insert('na3DA'); soma.gbar_na3DA = 0.027; soma.ena = 45; soma.ar2_na3DA = 1;         soma.insert('nap'); soma.gbar_nap = 0.000142;        soma.insert('kdrDA'); soma.gbar_kdrDA = 0.002; soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHPNE'); soma.gsAHPbar_sAHPNE = 1.5e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;class Cell_C:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma (self):        soma = self.soma        soma.nseg = 1        soma.diam = 24.75        soma.L = 25        soma.Ra = 200        soma.cm = 2.5        soma.insert('cadyn'); soma.eca = 120; soma.gcabar_cadyn = 5.5e-4;         soma.insert('leak'); soma.el_leak = -72; soma.glbar_leak = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5; # soma.ehd_hd = -43;        soma.insert('na3'); soma.ena = 45; soma.gbar_na3 = 0.03; soma.ar2_na3 = 1; # some.sh_na3 = 10;        soma.insert('nap'); soma.gbar_nap =  0.000142;        soma.insert('kdr'); soma.ek = -80; soma.gbar_kdr = 0.0035;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHP'); soma.gsAHPbar_sAHP = 1.15e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;		        for sec in h.allsec():            if h.ismembrane('kdr', sec=sec) or h.ismembrane('kdr', sec=sec):			    soma.ek = -80;            elif h.ismembrane('hd', sec=sec):                soma.ehd_hd = -43class Inter:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.dend = dend = h.Section(name='dend', cell=self)        self.dend.connect(self.soma,1,0)	#self.Rm = 20000        self.initsoma()        self.initdend()    def initsoma (self):        soma = self.soma        soma.nseg = 1;        soma.diam = 15.0;        soma.L = 15;        soma.Ra = 150*22.5;        soma.cm = 1;	        soma.insert('leakinter'); soma.el_leakinter = -70; soma.glbar_inter_leakinter = 5e-5; #1/Rm        soma.insert('nainter'); soma.ena = 45; soma.gnabar_nainter = 0.035;        soma.insert('kdrinter'); soma.ek = -80; soma.gkdrbar_kdrinter = 0.008;    def initdend (self):        dend = self.dend        dend.nseg = 1;        dend.diam = 10;        dend.L = 150;        dend.Ra = 150;        dend.cm = 1;        dend.insert('leakinter'); dend.el_leakinter = -70; dend.glbar_inter_leakinter = 5e-5; #1/Rm        dend.insert('nainter'); dend.ena = 45; dend.gnabar_nainter = 0.01;        dend.insert('kdrinter'); dend.ek = -80; dend.gkdrbar_kdrinter = 0.003;   class Cell_CDA:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma(self):        soma = self.soma        soma.nseg = 1        soma.diam = 24.75        soma.L = 25        soma.Ra = 200        soma.cm = 2.5        soma.insert('cadyn'); soma.gcabar_cadyn = 5.5e-4; soma.eca = 120;        soma.insert('leakDA'); soma.el_leakDA = -72; soma.glbar_leakDA = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5;        soma.insert('na3DA'); soma.gbar_na3DA = 0.03; soma.ena = 45; soma.ar2_na3DA = 1;         soma.insert('nap'); soma.gbar_nap = 0.000142;        soma.insert('kdrDA'); soma.gbar_kdrDA = 0.0035; soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHP'); soma.gsAHPbar_sAHP = 1.15e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;class Cell_CNE:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma(self):        soma = self.soma        soma.nseg = 1        soma.diam = 24.75        soma.L = 25        soma.Ra = 200        soma.cm = 2.5        soma.insert('cadyn'); soma.gcabar_cadyn = 5.5e-4; soma.eca = 120;        soma.insert('leak'); soma.el_leak = -72; soma.glbar_leak = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5;        soma.insert('na3'); soma.gbar_na3 = 0.03; soma.ena = 45; soma.ar2_na3 = 1;         soma.insert('nap'); soma.gbar_nap = 0.000142;        soma.insert('kdr'); soma.gbar_kdr = 0.0035; soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHPNE'); soma.gsAHPbar_sAHPNE = 1.15e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;class Cell_CDANE:    def __init__ (self):        self.soma = soma = h.Section(name='soma', cell=self)        self.initsoma()    def initsoma(self):        soma = self.soma        soma.nseg = 1        soma.diam = 24.75        soma.L = 25        soma.Ra = 200        soma.cm = 2.5        soma.insert('cadyn'); soma.gcabar_cadyn = 5.5e-4; soma.eca = 120;        soma.insert('leakDA'); soma.el_leakDA = -72; soma.glbar_leakDA = 5.5e-5;        soma.insert('hd'); soma.ghdbar_hd = 1.5e-5;        soma.insert('na3DA'); soma.gbar_na3DA = 0.03; soma.ena = 45; soma.ar2_na3DA = 1;         soma.insert('nap'); soma.gbar_nap = 0.000142;        soma.insert('kdrDA'); soma.gbar_kdrDA = 0.0035; soma.ek = -80;        soma.insert('capool'); soma.taucas_capool = 1000; soma.fcas_capool = 0.05;        soma.insert('sAHP'); soma.gsAHPbar_sAHP = 1.15e-4;        soma.insert('im'); soma.gbar_im = 6e-4;        soma.insert('kap'); soma.gkabar_kap = 0.002;